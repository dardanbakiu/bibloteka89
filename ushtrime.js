const user = {
    name: 'hamdi',
    surname: 'berisha',
    mosha: '20'
}

// const name = user.name
// const surname = user.surname

const { name, surname, mosha } = user

console.log(name, surname, mosha)